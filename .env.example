# DSpace Configuration
DSPACE_ENDPOINT=https://example.dspace.org
COMMUNITY_ID=123e4567-e89b-12d3-a456-426614174000
SUBCOMMUNITY_ID=123e4567-e89b-12d3-a456-426614174001
OUTPUT_FILE=production_summary.md

# Feature: Extract source code URLs from PDFs
# Set to 'true' to enable downloading PDFs and extracting source code repository URLs
EXTRACT_SOURCE_URLS=false

# Feature: Post-processing analysis with Ollama
# Set to 'true' to enable document text analysis using Ollama
ENABLE_OLLAMA_ANALYSIS=false
# Ollama API endpoint (e.g., http://localhost:11434)
OLLAMA_ENDPOINT=http://localhost:11434
# Ollama model to use for analysis (e.g., llama2, mistral, etc.)
OLLAMA_MODEL=llama2
# Optional: Specific model for embeddings (if different from OLLAMA_MODEL)
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# Feature: Save individual document outputs
# Set to 'true' to save individual summary and vector files for each document
SAVE_INDIVIDUAL_OUTPUTS=true
# Directory to save individual outputs (default: ./production)
PRODUCTION_OUTPUT_DIR=./production

# Feature: Post-processing Analysis
# Set to 'true' to enable clustering, network analysis, and visualization
ENABLE_POST_PROCESSING=false
# Output file for analysis results JSON
POST_PROCESSING_OUTPUT_JSON=analysis_results.json
# Output file for interactive HTML visualization
POST_PROCESSING_OUTPUT_HTML=visualization.html
# Clustering method: 'kmeans' or 'dbscan'
CLUSTERING_METHOD=kmeans
# Number of clusters (auto-determined if not set)
# N_CLUSTERS=5
